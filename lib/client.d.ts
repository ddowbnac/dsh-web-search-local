// Type stub for lib/client.js — the hand-emitted browser bundle (window.__ModuleLoader__
// lazy-CJS format). It registers itself on `window.__ModuleLoader__` at import time;
// tests import it for its side effects only, so no typed exports are declared.
export {};
